export const metadata = {
  title: 'Dentzy | Reset Password',
  description: 'Reset your password for the Dentzy Clinical Lab Portal.',
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}
