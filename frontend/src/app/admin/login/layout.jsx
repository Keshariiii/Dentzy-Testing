export const metadata = {
  title: 'Dentzy Admin | Portal Login',
  description: 'Log in to the Dentzy Admin Portal to manage the laboratory, users, and cases.',
};

export default function AdminLoginLayout({ children }) {
  return children;
}
